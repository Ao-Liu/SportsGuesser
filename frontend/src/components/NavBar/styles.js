import { styled } from '@mui/system'
import Button from '@mui/material/Button'

export const TouchBarBtn = styled(Button)(({ theme }) => ({
  marginLeft: '60px',
  fontSize: '18px',
  color: '#424242',
  fontFamily: 'Monospace'
}))
